package handlers

// AuthHandler handles authentication-related HTTP requests
// Add your custom auth handlers here if needed.
// CoreGo already provides basic auth handlers through core.Auth
type AuthHandler struct {
	// Add your dependencies here (e.g., database connection, services)
}

// NewAuthHandler creates a new auth handler
func NewAuthHandler() *AuthHandler {
	return &AuthHandler{}
}
